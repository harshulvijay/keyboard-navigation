{
  "name": "roving-tab-index",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:lib": "tsm scripts/build.ts && pnpm build:typedefs",
    "build:typedefs": "tsc --noEmit false --emitDeclarationOnly --declaration --outDir dist/typings",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/glob": "^7.2.0",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.13.8",
    "glob": "^7.2.0",
    "kleur": "^4.1.4",
    "sass": "^1.42.1",
    "tsm": "^2.1.2",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
